import React from 'react';
import Button from './Button';

const RegisterButton = () =>
    <Button redirectRoute='/register' className='btn btn-register' text='Click here to Register' />;

export default RegisterButton;